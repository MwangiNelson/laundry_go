"use client";

/**
 * @deprecated This component is no longer used. The services settings now use
 * the new schema. See service_form.tsx for the current implementation.
 */
export const LaundryServiceForm = (_props: { service: unknown }) => {
  return null;
};
